version: '3.8'

services:
  test-runner:
    build:
      context: .
      dockerfile: Dockerfile.runner
    volumes:
      - ../demo_codebase:/app
    environment:
      - NODE_ENV=test
    command: npm test
